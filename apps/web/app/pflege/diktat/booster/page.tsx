import { DiktatBooster } from "@/components/DiktatBooster";

export const metadata = { title: "Diktat-Booster · SIS-Klassifizierung" };

export default function BoosterPage() {
  return <DiktatBooster runden={10} />;
}
