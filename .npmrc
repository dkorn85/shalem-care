# Auch unter NODE_ENV=production die devDependencies installieren —
# tailwindcss, postcss, typescript & co werden zur BUILD-Zeit gebraucht.
# Ohne diese Zeile scheitert `next build` auf Hostinger / Render mit
# "Cannot find module 'tailwindcss'".
production=false
